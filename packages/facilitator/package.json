{
  "name": "kova-facilitator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup",
    "start": "node dist/index.js",
    "lint": "tsc --noEmit",
    "test": "cross-env FACILITATOR_STELLAR_SECRET=STEST KOVA_TREASURY_ADDRESS=GTEST vitest run",
    "clean": "rm -rf dist .turbo node_modules"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@stellar/stellar-sdk": "^13.1.0",
    "fastify": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "cross-env": "^10.1.0",
    "tsup": "^8.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.4"
  }
}
