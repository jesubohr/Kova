{
  "name": "kova",
  "version": "1.0.0",
  "private": true,
  "description": "Kova — Protect APIs from AI Agents behind x402 paywalls on Stellar",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "dotenv": "17.4.2",
    "prettier": "3.8.2",
    "turbo": "2.5.0",
    "typescript": "6.0.2"
  },
  "packageManager": "pnpm@10.8.1",
  "engines": {
    "node": ">=20"
  }
}
